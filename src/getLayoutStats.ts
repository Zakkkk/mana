import { LayoutStats, GlobalSettings, Layout } from "./types";

const getLayoutStats = (
  gs: GlobalSettings,
  layout: Layout,
  chosen: LayoutStats,
): LayoutStats => {
  const stats: LayoutStats = {};

  return stats;
};
export default getLayoutStats;
